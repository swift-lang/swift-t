// Documentation strings are optional: with none at all the usage message
// still reports the shape of the command line.

arguments(string username, int v);

trace("hello " + username, " the value is ", v+1);
