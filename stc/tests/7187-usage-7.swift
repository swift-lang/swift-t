// The program description may be given on its own, with no per-argument
// documentation.

arguments(string username,
          int v,
          description : "increment the value for the user");

trace("hello " + username, " the value is ", v+1);
