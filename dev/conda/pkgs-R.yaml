# pkgs-R.yaml: START
COMMENT( Extra packages for R.                              )m4_dnl
COMMENT( Retain indentation                                 )m4_dnl
`    - r=getenv(R_VERSION)'
# pkgs-R.yaml: STOP
