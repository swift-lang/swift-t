#!/bin/zsh

# Generate linear-adlb-*.tcl, a tcl-turbine-adlb test case

COUNT=$1
DURATION=$2

if [[ ${COUNT} == "" ]]
then
  print "Not given: COUNT"
  return 1
fi

if [[ ${DURATION} == "" ]]
then
  print "Not given: DURATION"
  return 1
fi

OUTPUT="linear-adlb-${COUNT}-${DURATION}.tcl"

# Header
{
  print
  print "# Generated by mk-linear-adlb.zsh"
  print "package require turbine 0.1"
  print "turbine_adlb_init"
  print
} > ${OUTPUT}

# Turbine data/rules section
{
  print "proc rules { } {"

  # Data declarations
  for (( i=0 ; i<COUNT ; i++ ))
  do
    print "\t turbine_file ${i} test/data/${i}.txt"
  done >> ${OUTPUT}

  # Task dependencies
  print "\t turbine_rule 0 0 {   } { 0 } { touch test/data/0.txt }"
  for (( i=1 ; i<COUNT ; i++ ))
  do
    PREV=$(( i-1 ))
    printf "\t turbine_rule ${i} ${i} { ${PREV} } { ${i} } "
    print "{ sleep ${DURATION} }"
  done
  print "}"
} >> ${OUTPUT}

# Footer
{
  print
  print "turbine_adlb rules"
  print
  print "turbine_finalize"
  print "adlb_finalize"
  print "puts OK"
} >> ${OUTPUT}

print "wrote: ${OUTPUT}"

return 0
