
DIR := src/util

TURBINE_SRC += $(shell find $(DIR) -name *.c)

$(DIR)/debug.h: $(DIR)/debug-tokens.tcl $(DIR)/debug-auto.tcl
	$(Q) "  TCL		$(@)"
	$(E) $(TCLSH_LOCAL) src/util/debug-auto.tcl

clean::
	$(E) rm -fv src/util/debug.h
