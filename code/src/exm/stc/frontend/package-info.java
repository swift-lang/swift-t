/**
 * The frontend package contains the classes that take the AST, perform
 * semantic analysis on it (e.g. typechecking, dataflow analysis) and then
 * generate the intermediate representation.
 */
package exm.stc.frontend;
