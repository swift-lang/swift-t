site=midway-westmere
app.midway-westmere.date=/bin/date

#
# Midway sites
#
site.midway-amd {
   jobManager=slurm
   jobQueue=amd
   tasksPerWorker=64
   initialScore=10000
   filesystem=local
   workdir=/scratch/midway/$USER/swiftwork
}

site.midway-gpu {
   jobManager=slurm
   jobQueue=gpu
   tasksPerWorker=16
   initialScore=10000
   filesystem=local
   workdir=/scratch/midway/$USER/work
}

site.midway {
   jobManager=slurm
   jobQueue=sandyb
   tasksPerWorker=16
   initialScore=10000
   filesystem=local
   workdir=/scratch/midway/$USER/work
}

site.midway-ssh {
   jobManager=midway.rcc.uchicago.edu:slurm
   tasksPerWorker=16
   jobQueue=sandyb
   initialScore=10000
   filesystem=local
   workdir=/scratch/midway/$USER/work
}

site.midway-sandyb {
   jobManager=slurm
   jobQueue=sandyb
   tasksPerWorker=16
   initialScore=10000
   filesystem=local
   workdir=/scratch/midway/$USER/work
}

site.midway-westmere {
   jobManager=slurm
   jobQueue=westmere
   tasksPerWorker=12
   initialScore=10000
   filesystem=local
   workdir=/scratch/midway/$USER/work
}

